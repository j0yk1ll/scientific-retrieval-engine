# Example environment variables for scientific-retrieval-engine
# Copy this file to `.env` and adjust values for your environment.
# Do NOT commit secrets or production credentials.

# PostgreSQL connection string used by the application and migrations
# Format: postgresql://user:password@host:port/dbname
RETRIEVAL_DB_DSN=postgresql://retrieval:retrieval@localhost:5432/retrieval

# Local directories for downloads and index data (relative or absolute)
RETRIEVAL_DATA_DIR=./data
RETRIEVAL_INDEX_DIR=./index

# GROBID service URL (HTTP endpoint)
RETRIEVAL_GROBID_URL=http://localhost:8070

# Contact email for Unpaywall API requests
RETRIEVAL_UNPAYWALL_EMAIL=you@example.com

# Default timeout (seconds) for outbound HTTP requests
RETRIEVAL_REQUEST_TIMEOUT_S=30.0

# Optional: set to 'true' to enable debugging for asyncio
# PYTHONASYNCIODEBUG=0
